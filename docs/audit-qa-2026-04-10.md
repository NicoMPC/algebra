# Audit QA extrême Matheux — 2026-04-10

**Auditeur** : Claude (session ultrathink)
**Scope** : sécurité, backend Edge Function, exos, emails, admin, agent auto, scénarios extrêmes
**Méthode** : lecture du code (Edge Function 1556 L, scripts Python), exécution des audits existants, script de fuzz/exploit `test_audit_qa_extreme.py` (créé), tests API en conditions réelles contre la prod Supabase

---

## 🚨 Résumé exécutif

- **1 exploit P0 exploité en live** : Stripe webhook sans signature — n'importe qui peut POST un payload forgé et **accorder le premium à n'importe quel email**. Confirmé par requête réelle (`test_stripe_webhook_forged` → `{"status":"success"}`).
- **1 fuite mdp P0** : `reset_password` stocke le mot de passe **en clair** dans `profiles.password_hash`.
- **3 bugs métier silencieux** impactant Leo (mode lite cassé), admin read-only (cassé), cron emails (équivalence stricte de jours).
- **1 bypass freemium MEDIUM** : le paramètre `source` est contrôlé par le client → `source:"BOOST"` contourne le verrou freemium.
- **Exos** : 339 "critiques" rapportés par `audit_exos.py` mais **la majorité sont des faux positifs** (fill exos flaggés comme "options=0"). À trier.
- **Agent auto J+1** : mécanisme validé, mais l'agent **n'est toujours pas schedulé** (manuel).

**Verdict pré-lancement** : ❌ **Ne pas lancer en massif avant d'avoir corrigé P0 #1 et #2**. Les autres bugs peuvent être tolérés quelques jours si < 10 élèves.

---

## 📋 Checklist priorisée

Légende : **P0** = bloquant lancement · **P1** = à fixer sous 48 h · **P2** = à surveiller

### P0 — Bloquant

| # | Problème | Fichier:ligne | Repro | Impact | Fix recommandé |
|---|---|---|---|---|---|
| 1 | Stripe webhook sans signature | `supabase/functions/api/index.ts:1538-1545` & `981-991` | `curl -X POST $API -d '{"type":"checkout.session.completed","data":{"object":{"customer_details":{"email":"victim@x.fr"}}}}'` → `status:success` + user devient premium | N'importe qui grante premium à tout email existant (fraude, vengeance, bypass paiement) | Vérifier `Stripe-Signature` header avec la clé secrète webhook Stripe. Utiliser `stripe.webhooks.constructEvent()` côté Deno (via esm.sh `stripe` lib) avant d'accepter le payload. |
| 2 | `reset_password` stocke mdp en clair | `index.ts:1028` | `.update({ password_hash: password })` | Quiconque lit `profiles` (admin, futur bug RLS) voit tous les mdp | Supprimer la ligne 1028. Le vrai hash est géré par Supabase Auth via `updateUserById`. `password_hash` dans profiles est legacy — nullifier systématiquement ou dropper la colonne. |
| 3 | Audit exos à trier | `docs/audit-exos-2026-04-10.md` | 339 "critiques" + 264 warnings | **Non chiffrable tant que non trié** — mais tout exo QCM/VF avec `options=[]` = exo cassé en prod | Patcher `audit_exos.py` pour ignorer `options` si `exo.type in ("fill",)`. Relancer. Traiter les vraies erreurs restantes. |

### P1 — À fixer sous 48 h

| # | Problème | Fichier:ligne | Impact | Fix |
|---|---|---|---|---|
| 4 | Mode lite cassé pour Leo (QETKY4) | `index.ts:477-478` & `639-640` | `profile.mode` n'est jamais sélectionné → `isLite` toujours `false` → Leo est bloqué par le freemium gate sur tous les chapitres sauf son `free_chapter` | Ajouter `mode` au SELECT dans les deux fonctions : `.select("email, mode, premium, premium_end, free_chapter")` |
| 5 | Admin read-only (A6) cassé | `index.ts:202, 337, 356` | `isAdminLogin` déclaré `false` et jamais réassigné → un login admin consomme `nextBoost`/`nextChapter` (mise à `null` dans `suivi`). Pour Nicolas pas grave (il n'a pas de contenu préparé pour lui), mais si un jour un admin secondaire existe → data loss silencieuse | Après récupération du profil : `isAdminLogin = !!profile.is_admin;` |
| 6 | `cronSendEmails` équivalence stricte des jours | `index.ts:1418` | `if (diffDays !== day) continue;` — si le cron saute 1 jour (week-end, panne), l'élève rate définitivement son J+3 / J+7 / J+14 | `if (diffDays < day) continue;` + vérifier via `email_logs` que `J+day` n'a jamais été envoyé (la dédup existe déjà dans `sendMarketingEmail` → compatible) |
| 7 | Bypass freemium via `source:"BOOST"` | `index.ts:493` & `649-658` | Le client contrôle `source` → depuis DevTools, envoyer `source:"BOOST"` sur n'importe quel chapitre bypass le verrou freemium | Valider côté serveur : `source === "BOOST"` ne doit être accepté que si un `daily_boosts` du jour existe ET que le `num_exo` est cohérent avec le boost. Idem `CALIBRAGE` : 1 seule fois à l'inscription. |
| 8 | Cron emails Supabase pas schedulé | infra Supabase | La séquence J+1/J+3/J+7/J+14 ne part pas toute seule — seul J+0 part à l'inscription. Les élèves qui ne reviennent pas ne reçoivent aucun email de relance | Ajouter `pg_cron` extension + schedule : `SELECT cron.schedule('matheux-emails', '0 9 * * *', $$select net.http_post('$API', '{"action":"cron_send_emails"}')$$);` OU GitHub Action quotidienne qui curl l'endpoint |

### P2 — À surveiller

| # | Problème | Fichier:ligne | Impact | Note |
|---|---|---|---|---|
| 9 | Identity check conditionnel | `index.ts:480` `if (p.email && ...)` | Defense in depth : sans `email` dans le payload, aucun check → si un attaquant devine un code 6-chars et pas l'email, il peut quand même polluer les scores | Rendre obligatoire, ou mieux : exiger un JWT Supabase Auth pour toutes les actions élève |
| 10 | `generateCode` sans exclusion de contexte | `index.ts:53-60` | 6 chars alphanum sans 0/1/I/O → 32^6 = 1 milliard, OK. Collision vérifiée par `uniqueCode()` qui lit TOUS les codes en mémoire → **O(n) à chaque register**, acceptable à <10k users | À surveiller > 50k users |
| 11 | `progress.upsert` avec onConflict incohérent | `index.ts:563` | `onConflict: "code,chapitre"` mais la table utilise `categorie` → l'upsert échoue silencieusement en race condition (catch vide L.565) | Harmoniser : `onConflict: "code,categorie"` si la contrainte DB existe |
| 12 | Reply-to incohérent entre templates | `index.ts:1157` | `resendSend` default `replyTo = "contact@matheux.fr"`, mais certains appels ne le passent pas → l'utilisateur répond à contact@ qui doit exister | Vérifier que `contact@matheux.fr` est une vraie boîte qui arrive à Nicolas |
| 13 | `login` lit `select("*")` sur profiles | `index.ts:218` | Retourne `password_hash` au client — le front ne l'utilise pas, mais c'est exposé dans le payload de login | Remplacer `*` par une whitelist explicite |
| 14 | `save_score` upsert `ignoreDuplicates: true` | `index.ts:518` | Si l'élève répond 2× le même exo le même jour (EASY puis HARD), le 1er écrit gagne. Intentionnel pour dedup. Si un jour on autorise "refaire un exo", il faut changer ça | OK MVP |
| 15 | Audit `verify_hints.py` : 1877 warnings | `docs/audit-hints-2026-04-10.md` | Qualité pédago dégradée sur beaucoup d'exos (indices dévoilent trop) | Tri manuel / regénération par chapitres prioritaires (3ème Brevet) |
| 16 | Ancien hash SHA-256 accepté en fallback login | `index.ts:206-213` | Si `hash.length === 64` le backend accepte directement l'égalité → si un attaquant récupère `password_hash` (via bug #2 ou un leak), il peut se logger avec le hash brut sans le mot de passe | Désactiver le fallback après migration complète |
| 17 | Anciens élèves (7) non migrés Supabase | mémoire `limites.md` | Auguste, Charlie, Jérôme, Johanna, Noam, Maxime — encore uniquement sur Google Sheets | Les contacter pour réinscription ou migration manuelle |
| 18 | Pas de rate limiting sur l'Edge Function | infra | Un attaquant peut spammer `register` ou `save_score` | Ajouter `Supabase Pro + Rate Limiter` ou rate-limit dans le code |

---

## 🧪 Tests exécutés

### Scripts existants

| Script | Résultat | Notes |
|---|---|---|
| `check_students.py` | ✅ 12/12 OK | 3 profils réels (Leo, Marine, Nicolas). Leo a 0 scores, Marine/Nicolas ont 5 sans Suivi |
| `audit_exos.py` | ⚠️ 339 crit + 264 warn | **Faux positifs** sur exos fill (voir P0 #3) |
| `verify_hints.py` | ⚠️ 1877 warnings | 1120 exos audités |
| `test_publishdate_proof.py` | ✅ | Mécanisme J+1 validé (publishDate + gate `>= todayStr`) |

### Script créé : `test_audit_qa_extreme.py`

12 checks, log `audit_logs/extreme-2026-04-10.jsonl`.

```
✅ 10 OK · ❌ 1 FAIL · ⚠️ 1 WARN

✅ action_inconnue            rejet propre
✅ register_invalid_email     format refusé
✅ register_invalid_level     niveau refusé
✅ save_score_invalid_result  résultat refusé
✅ save_score_missing_fields  champ manquant détecté
✅ save_score_short_code      code refusé
⚠️ identity_check_without_email   defense-in-depth manquant
✅ cors_options               headers OK
✅ generate_diagnostic_3eme   18 exos retournés
✅ forgot_password_unknown    pas d'enumeration
✅ login_wrong_creds          rejet
❌ stripe_webhook_forged      EXPLOIT P0 confirmé
```

Le FAIL est attendu et **doit rester rouge jusqu'à livraison du fix P0 #1**. Il servira de test de régression permanent.

---

## 📊 État des données (Supabase prod, 2026-04-10)

- **3 profils réels** : Nicolas (29UTUQ), Marine (KWQ8AP), Leo (QETKY4)
- **Leo** : 0 scores, 0 suivi, mode `lite` (cassé par P1 #4)
- **Marine & Nicolas** : 5 scores chacun, pas de Suivi, pas de boost
- **Aucun boost** dans `daily_boosts` pour les élèves réels → l'agent auto n'a pas tourné depuis la migration
- 4 profils test avaient été injectés le 07/04 (TS1INE, TS2HUG, TS3JAD, TS4ADA) mais ils n'apparaissent plus dans `check_students.py` → **à reconfirmer**

---

## ✅ Invariants vérifiés OK

- **G16 J+1 non négociable** : publish date gate `parsed.publishDate >= todayStr` fonctionne (login L.333 & L.352)
- **P1 Diagnostic 5 questions QCM/VF** : filtres back + front présents
- **CORS preflight** : OPTIONS traité proprement (L.1522-1530)
- **Dédup scores** : `onConflict: "code,chapitre,num_exo,date"` protège des doubles inserts
- **Email dédup** : `email_logs` avec check `J+{day}` et `statut=envoyé` (L.1365-1367)
- **Unsub** : `_isUnsubscribed()` via `email_logs.type=UNSUB` (L.1360-1362)
- **Templates emails J+0/1/3/7/14** : tous présents, design Outlook-safe, CTA Stripe présent
- **Freemium gate backend présent** (même s'il est contournable par `source`, la logique globale est là — L.493)

---

## 🛠️ Plan d'action suggéré (ordre de frappe)

1. **Maintenant, avant tout push Reddit/Maxime Lim** : fixer P0 #1 (Stripe signature) + P0 #2 (reset password)
2. **Dans la foulée** : fixer P1 #4 (mode lite Leo) — Leo est ton seul vrai élève, son parcours est cassé
3. **Avant scheduling admin-auto** : fixer P1 #5 (`isAdminLogin`) pour éviter data loss si un admin secondaire est ajouté
4. **Brancher le cron emails** (P1 #8) — sinon les élèves du lancement n'auront que J+0 et aucune relance
5. **Fixer `audit_exos.py`** (P0 #3) pour distinguer fill/qcm, relancer, traiter les vrais résidus
6. **P1 #7** : gater `source` côté serveur — attaque low-prio mais facile à corriger (10 lignes)
7. **P2** : à batcher après le lancement

---

## 📁 Artefacts

- `test_audit_qa_extreme.py` — script de régression permanent (racine repo)
- `audit_logs/extreme-2026-04-10.jsonl` — logs machine-readable
- `docs/audit-exos-2026-04-10.md` — audit qualité exos (rapport généré aujourd'hui)
- `docs/audit-hints-2026-04-10.md` — audit qualité indices
- Google Calendar event `🔍 Audit QA extrême Matheux — 2026-04-10` (ID `tqs743d1ch5ap8h0d3q9qo2994`)

---

**Fin du rapport.**
